<div class="system-environment" aria-hidden="true">
  <div class="system-environment__grid"></div>
  <div class="system-environment__glow system-environment__glow--one"></div>
  <div class="system-environment__glow system-environment__glow--two"></div>
  <div class="system-environment__network">
    <i class="system-node system-node--1"></i><i class="system-node system-node--2"></i>
    <i class="system-node system-node--3"></i><i class="system-node system-node--4"></i>
    <i class="system-node system-node--5"></i><i class="system-node system-node--6"></i>
    <i class="system-line system-line--1"></i><i class="system-line system-line--2"></i>
    <i class="system-line system-line--3"></i><i class="system-line system-line--4"></i>
  </div>
  <div class="system-environment__noise"></div>
</div>

<style>
  .system-environment { position: fixed; z-index: -2; inset: 0; overflow: hidden; pointer-events: none; }
  .system-environment__grid { position: absolute; inset: -10% -10% 0; opacity: .28; background-image: linear-gradient(rgba(108,151,255,.055) 1px, transparent 1px), linear-gradient(90deg, rgba(108,151,255,.055) 1px, transparent 1px); background-size: 64px 64px; mask-image: linear-gradient(to bottom, #000 0%, rgba(0,0,0,.72) 48%, transparent 94%); transform: perspective(700px) rotateX(58deg) translateY(-21%); transform-origin: 50% 0; }
  .system-environment__glow { position: absolute; border-radius: 50%; filter: blur(2px); opacity: .55; }
  .system-environment__glow--one { top: -28rem; left: 50%; width: 72rem; height: 55rem; background: radial-gradient(circle, rgba(20,77,218,.2), rgba(8,39,94,.06) 44%, transparent 70%); transform: translateX(-50%); }
  .system-environment__glow--two { top: 34%; left: -24rem; width: 54rem; height: 46rem; background: radial-gradient(circle, rgba(86,139,230,.1), transparent 69%); }
  .system-environment__network { position: absolute; top: 7rem; right: -1rem; width: min(42vw, 36rem); height: 25rem; opacity: .28; }
  .system-node, .system-line { position: absolute; display: block; }
  .system-node { z-index: 2; width: 5px; height: 5px; border: 1px solid rgba(137,181,255,.95); border-radius: 50%; background: #071021; box-shadow: 0 0 11px rgba(65,132,255,.85); animation: system-node-pulse 5s ease-in-out infinite; }
  .system-node--1 { top: 12%; left: 18%; }.system-node--2 { top: 35%; left: 54%; animation-delay: -1.2s; }.system-node--3 { top: 66%; left: 28%; animation-delay: -2.7s; }.system-node--4 { top: 78%; left: 76%; animation-delay: -3.5s; }.system-node--5 { top: 18%; left: 84%; animation-delay: -4.1s; }.system-node--6 { top: 48%; left: 96%; animation-delay: -2s; }
  .system-line { height: 1px; background: linear-gradient(90deg, rgba(84,144,255,.1), rgba(135,181,255,.58), rgba(84,144,255,.08)); transform-origin: left center; }
  .system-line--1 { top: 13%; left: 19%; width: 40%; transform: rotate(28deg); }.system-line--2 { top: 36%; left: 55%; width: 34%; transform: rotate(-22deg); }.system-line--3 { top: 67%; left: 29%; width: 51%; transform: rotate(12deg); }.system-line--4 { top: 19%; left: 84%; width: 35%; transform: rotate(74deg); }
  .system-environment__noise { position: absolute; inset: 0; opacity: .025; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 160 160' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.88' numOctaves='2' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.8'/%3E%3C/svg%3E"); }
  @keyframes system-node-pulse { 0%, 100% { opacity: .45; transform: scale(.8); } 50% { opacity: 1; transform: scale(1.35); } }
  @media (max-width: 760px) { .system-environment__grid { background-size: 48px 48px; opacity: .2; }.system-environment__network { right: -8rem; width: 26rem; opacity: .18; }.system-environment__glow--two { display: none; } }
  @media (prefers-reduced-motion: reduce) { .system-node { animation: none; } }
</style>
