---
import BaseLayout from '../layouts/BaseLayout.astro';
import { projects } from '../data/projects';
import { products } from '../data/products';
import ProjectSpotlight from '../components/ProjectSpotlight.astro';
import ProjectIndexItem from '../components/ProjectIndexItem.astro';

// Sort by sortOrder to maintain deliberate ecosystem narrative
const ordered = [...projects].sort((a, b) => (a.sortOrder ?? 99) - (b.sortOrder ?? 99));
const codeForge = ordered.find((p) => p.slug === 'codeforge')!;
const gems = ordered.find((p) => p.slug === 'gems-training-grounds')!;
const privateDev = ordered.filter((p) => p.slug !== 'codeforge' && p.slug !== 'gems-training-grounds');
const codeForgeProduct = products.find((p) => p.projectSlug === 'codeforge');
---
<BaseLayout
  title="Projects"
  description="Browse FDS projects across autonomous engineering, AI research, game creation, publishing, civic information, and local discovery."
>
  <div class="projects-hero">
    <div class="projects-hero__backdrop" aria-hidden="true">
      <svg viewBox="0 0 800 400" preserveAspectRatio="xMidYMid slice">
        <!-- Subtle orbital curves -->
        <ellipse cx="400" cy="200" rx="320" ry="120" fill="none" stroke="rgba(79,143,255,.08)" stroke-width="1" opacity=".6" />
        <ellipse cx="400" cy="200" rx="240" ry="80" fill="none" stroke="rgba(47,107,255,.06)" stroke-width="1" opacity=".5" />
        <ellipse cx="400" cy="200" rx="160" ry="50" fill="none" stroke="rgba(79,143,255,.04)" stroke-width="1" opacity=".4" />
        <!-- Decorative constellation points -->
        <circle cx="120" cy="80" r="1.5" fill="rgba(79,143,255,.35)" />
        <circle cx="680" cy="60" r="1" fill="rgba(180,135,255,.3)" />
        <circle cx="750" cy="180" r="1.2" fill="rgba(118,183,125,.25)" />
        <circle cx="60" cy="320" r="1" fill="rgba(135,146,168,.28)" />
        <circle cx="600" cy="350" r="1.5" fill="rgba(217,129,63,.32)" />
      </svg>
    </div>
    <p class="projects-hero__kicker">FDS // SYSTEM DIRECTORY</p>
    <h1 class="projects-hero__title">Projects</h1>
    <p class="projects-hero__description">
      Browse the software and research FDS is actively developing—from autonomous engineering and model learning
      to game creation, publishing, civic information, and nearby food discovery.
    </p>
  </div>

  <section class="ecosystem-overview" aria-labelledby="ecosystem-heading">
    <h2 id="ecosystem-heading" class="sr-only">FDS Technology Ecosystems</h2>
    <div class="ecosystem-overview__content">
      <p class="ecosystem-intro">
        Projects are what FDS is building. Some are research programs, some are private products, and some are already released through Forged.
      </p>
      <div class="ecosystem-grid">
        <a href="#codeforge" class="ecosystem-item" data-ecosystem="engineering">
          <span class="ecosystem-dot" aria-hidden="true"></span><div class="ecosystem-text"><h3>Engineering</h3><p>Autonomous repository work</p></div>
        </a>
        <a href="#gems-training-grounds" class="ecosystem-item" data-ecosystem="intelligence">
          <span class="ecosystem-dot" aria-hidden="true"></span>
          <div class="ecosystem-text">
            <h3>Intelligence</h3>
            <p>AI research, training systems</p>
          </div>
        </a>
        <a href="#kyrablox" class="ecosystem-item" data-ecosystem="gaming">
          <span class="ecosystem-dot" aria-hidden="true"></span>
          <div class="ecosystem-text">
            <h3>Gaming</h3>
            <p>Game development tooling</p>
          </div>
        </a>
        <a href="#kayla-ai-publisher" class="ecosystem-item" data-ecosystem="publishing">
          <span class="ecosystem-dot" aria-hidden="true"></span>
          <div class="ecosystem-text">
            <h3>Publishing</h3>
            <p>Creative media workflows</p>
          </div>
        </a>
        <a href="#we-the-people" class="ecosystem-item" data-ecosystem="civic">
          <span class="ecosystem-dot" aria-hidden="true"></span>
          <div class="ecosystem-text">
            <h3>Civic</h3>
            <p>Public information access</p>
          </div>
        </a>
        <a href="#farmstand-finder" class="ecosystem-item" data-ecosystem="foraging">
          <span class="ecosystem-dot" aria-hidden="true"></span>
          <div class="ecosystem-text">
            <h3>Foraging</h3>
            <p>Local food discovery</p>
          </div>
        </a>
      </div>
    </div>
  </section>

  <section class="projects-section" aria-labelledby="projects-list">
    <h2 id="projects-list" class="sr-only">Project Directory</h2>

    <div class="directory-tier">
      <p class="directory-tier__label">Featured / Released</p>
      <ProjectSpotlight
        project={codeForge}
        tier="Public product and FDS engineering foundation"
        secondaryCta={codeForgeProduct ? { label: 'Get CodeForge', href: '/forged' } : undefined}
      />
    </div>

    <div class="directory-tier">
      <p class="directory-tier__label">Active Research</p>
      <ProjectSpotlight project={gems} tier="Flagship AI research program" />
    </div>

    <div class="directory-tier">
      <p class="directory-tier__label">Private Development</p>
      <p class="directory-tier__note">Earlier-stage work, not yet public. Status labels below reflect where each project actually is.</p>
      <ul class="directory-list">
        {privateDev.map((project) => <ProjectIndexItem project={project} />)}
      </ul>
    </div>
  </section>
</BaseLayout>

<style>
  .sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; clip-path: inset(50%); }
  
  .projects-hero {
    position: relative;
    max-width: 56rem;
    padding: clamp(3rem, 8vw, 5rem) 0 2rem;
  }
  
  .projects-hero__backdrop {
    position: absolute;
    inset: -2rem -4rem;
    z-index: -1;
    opacity: .7;
    pointer-events: none;
  }
  
  .projects-hero__backdrop svg {
    width: 100%;
    height: 100%;
  }
  
  .projects-hero__kicker {
    margin-bottom: .75rem;
    color: var(--color-accent-bright);
    font: 700 .68rem/1 var(--font-mono);
    letter-spacing: .16em;
    text-transform: uppercase;
  }
  
  .projects-hero__title {
    margin: 0 0 1rem;
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    line-height: .98;
    letter-spacing: -.04em;
    color: var(--color-text);
  }
  
  .projects-hero__description {
    max-width: 42rem;
    margin: 0;
    color: var(--color-text-muted);
    font-size: clamp(1rem, 2vw, 1.1rem);
    line-height: 1.7;
  }
  
  .projects-hero__description strong {
    color: var(--color-text);
    font-weight: 650;
  }

  .ecosystem-overview {
    position: relative;
    padding: 1.5rem;
    margin: 2rem 0 2.5rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, rgba(47,107,255,.04) 0%, rgba(9,14,23,.95) 50%);
    overflow: hidden;
  }
  
  .ecosystem-overview::before {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(79,143,255,.3), transparent);
    content: '';
  }

  .ecosystem-intro {
    max-width: 48rem;
    margin: 0 0 1.25rem;
    color: var(--color-text-muted);
    font-size: .92rem;
    line-height: 1.6;
  }

  .ecosystem-grid {
    display: flex;
    flex-wrap: wrap;
    gap: .5rem;
  }

  .ecosystem-item {
    display: flex;
    align-items: center;
    gap: .6rem;
    padding: .6rem .85rem;
    border: 1px solid transparent;
    border-radius: 999px;
    background: rgba(255,255,255,.02);
    text-decoration: none;
    transition: border-color .2s ease, background .2s ease;
  }
  
  .ecosystem-item:hover {
    background: rgba(255,255,255,.04);
    border-color: var(--color-border);
  }
  
  .ecosystem-item:focus-visible {
    outline: 2px solid var(--color-accent-bright);
    outline-offset: 2px;
  }

  .ecosystem-dot {
    flex-shrink: 0;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    box-shadow: 0 0 6px currentColor;
  }
  
  .ecosystem-item[data-ecosystem="intelligence"] { color: #4f8fff; }
  .ecosystem-item[data-ecosystem="engineering"] { color: #5a82e8; }
  .ecosystem-item[data-ecosystem="engineering"] .ecosystem-dot { background: #5a82e8; }
  .ecosystem-item[data-ecosystem="intelligence"] .ecosystem-dot { background: #4f8fff; }
  .ecosystem-item[data-ecosystem="gaming"] { color: #b487ff; }
  .ecosystem-item[data-ecosystem="gaming"] .ecosystem-dot { background: #b487ff; }
  .ecosystem-item[data-ecosystem="publishing"] { color: #d9813f; }
  .ecosystem-item[data-ecosystem="publishing"] .ecosystem-dot { background: #d9813f; }
  .ecosystem-item[data-ecosystem="civic"] { color: #8792a8; }
  .ecosystem-item[data-ecosystem="civic"] .ecosystem-dot { background: #8792a8; }
  .ecosystem-item[data-ecosystem="foraging"] { color: #76b77d; }
  .ecosystem-item[data-ecosystem="foraging"] .ecosystem-dot { background: #76b77d; }

  .ecosystem-text h3 {
    margin: 0;
    font-size: .72rem;
    font-weight: 700;
    letter-spacing: .06em;
    text-transform: uppercase;
  }
  
  .ecosystem-text p {
    margin: .15rem 0 0;
    color: var(--color-text-muted);
    font-size: .7rem;
    line-height: 1.3;
  }

  .directory-tier {
    margin-bottom: 2.5rem;
  }

  .directory-tier__label {
    margin: 0 0 .9rem;
    color: var(--color-text-subtle);
    font: 700 .68rem/1 var(--font-mono);
    letter-spacing: .14em;
    text-transform: uppercase;
  }

  .directory-tier__note {
    max-width: 42rem;
    margin: -.5rem 0 1rem;
    color: var(--color-text-subtle);
    font-size: .85rem;
    line-height: 1.6;
  }

  .directory-list {
    display: grid;
    gap: 1px;
    margin: 0;
    padding: 0;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-border);
    box-shadow: var(--shadow-sm);
    list-style: none;
    overflow: hidden;
  }

  .projects-section {
    padding-bottom: 4rem;
  }

  @media (max-width: 700px) {
    .ecosystem-grid {
      gap: .4rem;
    }
    .ecosystem-item {
      flex: 1 1 calc(50% - .25rem);
      min-width: 0;
      justify-content: flex-start;
      padding: .5rem .65rem;
    }
    .ecosystem-text p {
      display: none;
    }
  }
  
  @media (max-width: 480px) {
    .projects-hero {
      padding: 2rem 0 1.5rem;
    }
  }
</style>
